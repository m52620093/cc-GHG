# GHGリスト抽出・インポートファイル生成 仕様書

## 1. 概要

`ghgリスト.xlsx` から特定カラムを抽出し、外部の `setting.json` の設定に従って、既存のインポートファイル(テンプレート)を元に新しいインポート用Excelファイルを生成するツール。将来的にタスクスケジューラによる定期自動実行を想定する。

- 開発言語: Python
- パッケージ管理: uv
- 設定ファイル形式: JSON (`setting.json`)

## 2. 実行環境・配布形態

- 開発は uv 管理の Python プロジェクトとして行うが、最終的に PyInstaller 等を用いて Windows 向けの単一 exe ファイルにビルドする(実行環境に Python/uv が入っていない PC でもタスクスケジューラから実行できるようにするため。PyInstaller / Windows 64bit で確定)
- 実行ファイル(exe)・GHGリストファイル・インポートテンプレートファイル・`setting.json` は、いずれも同一フォルダに配置する想定(3節参照)
- タスクスケジューラでの実行は、対話セッション(ユーザーがログオン中)・非対話セッション(ログオフ中を含む)の両方を想定する。どちらのモードで実行するかは自動判定せず、`setting.json` の設定値で明示的に切り替える(8.2節参照)

## 3. ファイル配置・パス解決

- GHGリストファイル、インポートテンプレートファイル、`setting.json`、出力ファイル、ログファイル(`error.log`)は、すべて **アプリケーション(exe)が格納されているフォルダと同じフォルダ** に配置する
- 実行時のカレントディレクトリはタスクスケジューラの実行設定によって変わり得るため、カレントディレクトリには依存せず、実行ファイル自身のパス(exe化後は `sys.executable`、スクリプト実行時は `sys.argv[0]` 相当)から自分のフォルダを取得し、それを基準ディレクトリとして各ファイルパスを解決する
- `setting.json` 内のファイルパスは、上記基準ディレクトリからの相対パス(ファイル名のみ)で指定することを基本とする

## 4. 全体処理フロー

1. 実行ファイルと同じフォルダの `setting.json` を読み込む
2. GHGリストファイル(`source.file_path`)を開き、設定された範囲(開始位置〜終了カラム、部材図面列が空になるまでの行)を読み取る
3. 読み取った行を「部材図面」単位でグルーピングする
4. グループごとに、後述の優先順位付けロジックに従って採用する1行を決定する(採用不可の場合は除外しカウントする)
5. 採用した行から「部材図面」「GHG排出量単位毎」を抽出する(品名はGHGリストからの抽出対象外)
6. インポートテンプレートファイル(`import.template_file_path`)を開く
7. 設定された書き込み開始位置・カラムマッピングに従って、抽出した全部材図面分のデータを書き込む(固定値カラムがあれば同時に設定)
8. 設定された出力ファイル名で、テンプレートとは別ファイルとして保存する(参照元テンプレートは変更しない)
9. 除外件数・エラー有無をまとめ、完了通知ダイアログを表示する(8節参照)

## 5. 入力ファイル仕様(GHGリスト.xlsx)

| 項目 | 内容 |
|---|---|
| シート名 | `setting.json` で指定(既定値: `Sheet1`) |
| ヘッダー行 | `setting.json` で指定(既定値: 12行目) |
| データ開始行 | `setting.json` で指定(既定値: 13行目) |
| 読み取り開始列〜終了列 | `setting.json` で指定(既定値: C列〜AI列) |
| 読み取り終了行の判定 | 「部材図面」列(主キー列)が空になった行で読み取りを終了する |

抽出対象として参照する列(列位置は `setting.json` で管理し、フォーマット変更に追従できるようにする):

| 論理名 | 用途 | 出力対象 |
|---|---|---|
| 部材図面 | 主キー。文字列として抽出 | ○(出力する) |
| GHG排出量算定パターン | 同一部材図面が重複する場合の優先順位付けに使用 | ×(参照のみ) |
| 登録日時 | 同一部材図面・同一パターンが重複する場合の優先順位付けに使用(新しい日時を優先) | ×(参照のみ) |
| GHG排出量単位毎 | 数値(小数)として抽出。必須項目 | ○(出力する) |

上記以外の列(バイヤーコード、割戻し実施者コード、適用原単位種類名称、適用原単位番号、品名、サプライヤーコード、サプライヤー名、製造元コード、製造元名、サプライヤー係数y値、GHG排出量年間総量、Per、適用原単位(その他)、適用原単位Ver.、適用原単位名、適用原単位絶対値、適用原単位の単位、備考、企業コード、図#、バイヤー企業コード、バイヤー図#、サプライヤー企業コード、サプライヤー図#、連絡先、連絡先ベンダーコード、製造者型番、製造者品名、図番廃止日)は抽出不要(品名も抽出不要に変更)。

## 6. データ集約・優先順位付けロジック

「部材図面」が同一の行が複数存在する場合、以下の手順で1行を採用する。

1. 対象の部材図面を持つ全行を候補とする
2. 候補を以下の優先順で並び替える
   1. **GHG排出量算定パターンの優先度**(降順): パターン名の数値部分を比較し、数値が大きいほど優先度が高い(例: `パターン4A` は数値部分 `4` として扱う)
      - 優先順位(数値が大きい順): `パターン5` > `パターン4A` > `パターン3` > `パターン1`
      - 優先順位ルールは `setting.json` で管理し、将来のパターン追加・変更に対応する
   2. **登録日時**(降順): 同一パターンの行が複数ある場合、登録日時が新しいものを優先
3. 並び替えた候補を先頭から順に見ていき、「GHG排出量単位毎」に値が入っている最初の行を採用する
4. 採用した行から「部材図面」「GHG排出量単位毎」を抽出する
5. 候補の全行で「GHG排出量単位毎」が空だった場合、当該部材図面は出力対象から**除外**し、`error.log` に警告として記録する。除外件数は完了通知ダイアログで通知する(8節参照)

## 7. 出力ファイル仕様(インポートファイル)

| 項目 | 内容 |
|---|---|
| テンプレートファイル名 | `PurchasedProductFootprints_Template.xlsx`(実行ファイルと同じフォルダに配置) |
| シート名 | `PurchasedProductFootprints` |
| データ書き込み開始行 | 4行目(1〜3行目はテンプレート側のヘッダー・説明行のため変更しない)。実行のたびに同じ開始行から上書きする(既存データの最終行検出は行わない) |
| 出力ファイル名 | `setting.json` で指定(既定: `import_{yyyyMMdd}.xlsx`)。テンプレートファイルは変更せず、常に別ファイルとして新規出力する |

抽出データ・固定値の書き込み列は以下の通り(列位置は `setting.json` で管理し、テンプレート変更に追従できるようにする):

| 列 | 項目名(テンプレート) | 設定内容 |
|---|---|---|
| B列 | Product_identifier(製品ID) | GHGリストから抽出した「部材図面」 |
| E列 | uomSource_code(数量ディメンションコード) | 固定値 `PRODUOM` |
| F列 | calculationMethod_code(GHG計算方法コード) | 固定値 `AVD` |
| I列 | validityPeriod_validFrom(有効開始) | 固定値 `2000-01-01` |
| J列 | validityPeriod_validTo(有効終了) | 固定値 `2027-03-31` |
| K列 | productQuantity(製品数量) | 固定値 `1`(数値) |
| M列 | co2eQuantity(気候変動CO2e(キログラム)) | GHGリストから抽出した「GHG排出量単位毎」 |
| R列 | GHGCategory_code(GHGカテゴリコード) | 固定値 `3.01`(テキストとして設定。テンプレート側の項目定義がテキスト形式のため) |

上記以外の列(A, C, D, G, H, L, N〜Q)には値を設定しない。「品名」はGHGリストからの抽出対象外のため、テンプレートへの書き込みも行わない。

**【参考】** テンプレートファイルには `ValueHelp` シートの入力規則(ドロップダウン等)が設定されている。本ツールはopenpyxlでワークブック全体を開いて保存するため、読み込み時に「Data Validation extension is not supported and will be removed」という警告が出ることがあり、出力ファイルではその入力規則(ドロップダウンの選択肢制御)が失われる可能性がある。値そのものは正しく書き込まれるため機能上の影響はないが、出力ファイルを見た目上編集する用途がある場合は留意すること。

## 8. エラーハンドリング・完了通知

### 8.1 ログ(error.log)

- 実行ファイルと同じフォルダに `error.log` を出力する
- 形式: `[発生日時] [ERROR|WARN] メッセージ` を1行ずつ追記する
- 記録対象:
  - ERROR: GHGリスト/テンプレートファイルが見つからない、「GHG排出量単位毎」を数値変換できない、その他読み込み・書き込みに失敗した場合など(処理は中断する)
  - WARN: 「GHG排出量単位毎」が全候補で空のため部材図面を除外した場合など(処理は継続する)
- **ローテーション**: `error.log` が一定のファイルサイズに達したら、既存ログを `error.log.1` にリネームし、新しい `error.log` に書き込みを継続する一般的なサイズベースローテーション方式とする(Python標準の `logging.handlers.RotatingFileHandler` 相当)
  - 既定値: 1ファイルあたり最大 1MB、保持世代数 5(`error.log.1`〜`error.log.5`。上限を超えた最古のファイルは削除)
  - 上限値・保持世代数は `setting.json` の `log` セクションで変更可能

### 8.2 完了通知ダイアログ

- `setting.json` の `notification.interactive_mode` で、実行モードを明示的に切り替える
  - `true`(対話セッションで実行): 全処理完了後(または中断時)、除外した部材図面の件数、およびエラー・警告が発生していた場合はその旨(「error.log をご確認ください」)をまとめたメッセージボックスを表示する。ユーザーの応答(OKクリック等)を待つ通常の `MessageBox` でよい(タイムアウトによる自動クローズは不要)
  - `false`(非対話セッションで実行): メッセージボックスは一切表示しない。処理結果はすべて `error.log` にのみ記録する
- どちらのモードで実行するかはタスクの実行設定(ユーザーがログオンして手動実行するか、ログオフ中もタスクスケジューラで自動実行するか)に応じて、利用者が `setting.json` の値を設定する運用とする

## 9. 対象外事項

- GHGリストのシートが複数存在する場合の対応(現状 `Sheet1` のみ確認済み)
- インポートファイルへの書き込み時の既存データとの重複チェック(「同一部材図面を複数回の実行で二重に追記しない」という要件は、本仕様では「毎回固定開始行から全件上書き」という運用でカバーされるため、別途の重複チェック機構は設けない)
- 対話/非対話モードの自動判定(`setting.json` による明示的な切り替えのみとし、実行セッションの種類をプログラムが自動検出する機能は設けない)

## 10. setting.json スキーマ(案)

```json
{
  "source": {
    "file_path": "ghgリスト.xlsx",
    "sheet_name": "Sheet1",
    "header_row": 12,
    "data_start_row": 13,
    "start_column": "C",
    "end_column": "AI",
    "columns": {
      "部材図面": "E",
      "GHG排出量算定パターン": "F",
      "登録日時": "I",
      "GHG排出量単位毎": "Q"
    }
  },
  "pattern_priority": {
    "rule": "numeric_desc",
    "order": ["パターン5", "パターン4A", "パターン3", "パターン1"]
  },
  "import": {
    "template_file_path": "PurchasedProductFootprints_Template.xlsx",
    "sheet_name": "PurchasedProductFootprints",
    "data_start_row": 4,
    "column_mapping": {
      "部材図面": "B",
      "GHG排出量単位毎": "M"
    },
    "fixed_values": {
      "E": "PRODUOM",
      "F": "AVD",
      "I": "2000-01-01",
      "J": "2027-03-31",
      "K": 1,
      "R": "3.01"
    },
    "output_file_name": "import_{yyyyMMdd}.xlsx"
  },
  "notification": {
    "interactive_mode": true
  },
  "log": {
    "file_name": "error.log",
    "max_bytes": 1048576,
    "backup_count": 5
  }
}
```

## 11. 残課題(要確認事項まとめ)

現時点で未確定の事項はなし。実装フェーズに進む。
